  \begin{equation}\begin{gathered}\begin{aligned}
        \vFunc_{\arvl} & = \Ex_{\arvl}[\vFunc_{\dcsn}(\overbrace{\kNrm \RNrmByG + \tranShkEmp}^{\mNrm})]  \label{eq:vArvl}
%%% Unified: 𝒜(xₐ): v_arvl = 𝔼_arvl[𝒱(k·(R/Γ) + θ)] — arrival value; gₐᵥ: (k, θ) → m; Γ^{1−ρ} from normalization
\UnifiedNote{𝒜(xₐ): v_arvl = 𝔼_arvl[𝒱(k·(R/Γ) + θ)] — arrival value; gₐᵥ: (k, θ) → m; Γ^{1−ρ} from normalization}
      \end{aligned}\end{gathered}\end{equation}
