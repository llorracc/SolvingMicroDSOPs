  \begin{equation}\begin{gathered}\begin{aligned}
        \Rport_{\prdt+1}  & = \Rfree(1-\stigma_{\prdt}) + \Risky_{\prdt+1}\stigma_{\prdt} \label{eq:return1}
        \\               & = \Rfree + (\Risky_{\prdt+1}-\Rfree) \stigma_{\prdt} %\label{eq:return2}
\UnifiedNote{R̃_port = R + (R̃ − R)·ς — portfolio return definition; gᵥₑ: (xᵥ, ς) → xₑ via Rport}
      \end{aligned}\end{gathered}\end{equation}
